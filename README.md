# Audio Player - Функціональний програвач аудіофайлів

Сучасний десктоп програвач аудіофайлів для Windows з приємним UI та широкою підтримкою форматів.

## Особливості

- 🎵 Підтримка більшості аудіо форматів (MP3, WAV, FLAC, OGG, M4A, AAC та інші)
- 🎨 Сучасний та приємний інтерфейс
- 📋 Плейлист з можливістю додавання/видалення треків
- 🔄 Режими відтворення (Repeat, Shuffle)
- 🔊 Контроль гучності
- ⏯️ Повний контроль відтворення (Play, Pause, Stop, Next, Previous)
- 📊 Візуалізація прогресу відтворення
- 🎛️ Базова еквалайзер функціональність

## Встановлення

### Варіант 1: Автоматичне встановлення (Windows)
1. Встановіть Python 3.8 або новіший
2. Подвійний клік на `install.bat` - автоматично встановить всі залежності

### Варіант 2: Вручну через командний рядок
1. Встановіть Python 3.8 або новіший
2. Встановіть залежності:
```bash
python -m pip install -r requirements.txt
```

**Примітка:** Якщо команда `pip` не працює, використовуйте `python -m pip` замість неї.

## Запуск

### Варіант 1: Через батник (рекомендовано для Windows)
Просто подвійний клік на `run.bat` - файл автоматично:
- Перевірить наявність Python
- Встановить залежності якщо потрібно
- Запустить програму

### Варіант 2: Через командний рядок
```bash
python main.py
```

### Варіант 3: Тихий запуск (без виводу)
Подвійний клік на `run_silent.bat` - запуск без додаткових повідомлень

## Тестування

```bash
pytest tests/
```

## Структура проекту

```
Audio/
├── main.py              # Головний файл запуску
├── run.bat              # Батник для запуску (Windows)
├── run_silent.bat        # Тихий запуск (Windows)
├── install.bat           # Встановлення залежностей (Windows)
├── player/
│   ├── __init__.py
│   ├── audio_player.py  # Основний клас програвача
│   ├── playlist.py      # Управління плейлистом
│   └── ui/
│       ├── __init__.py
│       └── main_window.py  # Головне вікно UI
├── tests/               # Тести
└── requirements.txt
```

